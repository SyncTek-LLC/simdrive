# SpecterQA / SimDrive — PyPI Long-Description Draft (superseded)

> **STATUS: SUPERSEDED.** This draft predates the `simdrive` rebrand — old
> product name, an install line and MCP config entry for the pre-rebrand
> package name, an old total-tool count well below the current 36, and a
> link to the pre-rename GitHub repo. The actual PyPI `long_description` is
> `simdrive/README.md` — this file is not it and never was wired up as it.
> Kept only as historical scaffolding; do not cite or republish it.
