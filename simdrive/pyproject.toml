[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "specterqa-ios"
version = "17.0.0a1"
description = "SpecterQA for iOS — MCP-native iOS simulator driver. Vision-first, agent-driven."
readme = "README.md"
license = "MIT"
requires-python = ">=3.10"
authors = [
    {name = "SyncTek LLC", email = "info@synctek.io"},
]
keywords = ["ios", "simulator", "mcp", "claude", "testing", "qa", "agent", "anthropic"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Operating System :: MacOS",
    "Topic :: Software Development :: Testing",
    "Topic :: Software Development :: Quality Assurance",
]
dependencies = [
    "Pillow>=10.0",
    "pyyaml>=6.0",
    "mcp>=1.0",
    "pyobjc-framework-Quartz>=10.0",
    "pyobjc-framework-Vision>=10.0",
]

[project.urls]
Homepage = "https://synctek.io/specterqa"
Repository = "https://github.com/SyncTek-LLC/specterqa-ios"
Issues = "https://github.com/SyncTek-LLC/specterqa-ios/issues"

[project.optional-dependencies]
ssim = ["scikit-image>=0.22"]
dev = [
    "pytest>=7.0",
    "pytest-asyncio>=0.23",
    "ruff>=0.1.0",
]

[project.scripts]
specterqa-ios = "specterqa_ios.server:serve"
simdrive = "specterqa_ios.server:serve"
simdrive-mcp = "specterqa_ios.server:serve"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
specterqa_ios = ["_bin/simdrive-input"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
markers = [
    "live: tests that require a booted iOS Simulator and Xcode",
]

[tool.ruff]
line-length = 110
target-version = "py310"
