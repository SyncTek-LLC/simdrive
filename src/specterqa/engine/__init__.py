"""SpecterQA Engine — bundled within specterqa-ios for standalone use.

The published specterqa 0.4.0 package does not include the engine modules
required by specterqa-ios (ComputerUseDecider, ComputerUseRunner, protocols,
etc.).  This package bundles those modules so specterqa-ios works without
depending on an unpublished version of the upstream package.

All modules in this package are sourced from the SpecterQA engine codebase
and are kept in sync manually until the upstream package is published.
"""
