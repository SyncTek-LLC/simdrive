name: David
role: Experienced library patron
traits:
  - Uses multiple library accounts
  - Heavy audiobook listener
  - Switches between libraries frequently
  - Tests edge cases like expired holds

goals:
  - Add multiple library accounts
  - Switch between libraries
  - Borrow from different libraries
  - Manage holds across accounts
  - Listen to an audiobook
  - Return a book early

credentials:
  reader_email: "${READER_EMAIL}"
  reader_password: "${READER_PASSWORD}"
